# Context Hub 协作约定

本仓库是 Context Hub（CH，上下文中心）的独立仓库。当前阶段以本仓库文档为 CH 事实来源，父项目 QuarkfanTools 是集成方。改动前请先阅读：

1. `STATUS.md`
2. `docs/context-hub.md`
3. `docs/implementation-blueprint.md`
4. `docs/session-context-projection.md` when sessions, context projection, compaction or memory promotion are involved.

边界约定：

- CH 管理运行时可以合法使用的上下文，包括显式知识、短期记忆、中期记忆、长期记忆、派生摘要、索引、实体、关系、freshness、权限和审计记录。
- CH 对外提供上下文召回能力和记忆读写治理能力，不负责最终回复、不执行工具、不直接管理模型 provider。
- 知识库、RAG、Skill `knowledge/`、飞书文档/Wiki/云盘、本地文件知识库、客户知识库、会话摘要、用户偏好和项目记忆都属于 CH 可管理范围。
- 长期记忆写入必须经过候选、确认、策略或多证据强化流程；不能把模型输出直接当作长期事实。
- 任意召回和记忆写入都必须保留来源、scope、freshness、授权判定、置信度和审计线索。
- 涉及平台总边界、跨中心接口或 QuarkfanTools 集成状态时，同步父项目对应文档。
- 不得提交 API Key、App Secret、Token、用户消息正文、未脱敏知识内容、未脱敏记忆内容或未脱敏日志。

决策协作：

- 顶级工程原则：如果某个部分已经有成熟、优秀、许可合适且可维护的开源实现，应先做认真评估，再决定复用、适配、借鉴或自研，不要默认自己造轮子。
- 复用开源不等于把开源项目完整抄进来；粒度可以很大，例如直接把整个开源项目作为依赖或子系统，也可以很小，例如只借鉴某个模块设计、接口模型、状态机、测试方法或少量许可允许的代码片段。选择粒度时必须同时考虑隔离、安全、交付、自包含、授权、性能、维护成本和产品边界。
- 最终产品和架构决策由 Dean 做出。
- 不涉及产品方向、仓库归属、发布范围、对外承诺或不可逆架构选择时，Codex 拥有独立判断和建议权。
- Codex 必须主动思考、主动发现问题、主动提出风险、取舍、替代方案和低风险改进建议；不能只被动执行明确指令。
